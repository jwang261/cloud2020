server:
  port: 3377
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8840
      config:
        server-addr: 127.0.0.1:8840
        file-extension: yml
        group: DEFAULT_GROUP
        namespace: 826877f4-865b-4907-a188-fbaeb5305096

# ${spring.application.name}-​${spring.profile.active}.​${spring.cloud.nacos.config.file-extension}